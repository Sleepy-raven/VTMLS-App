spring.application.name=auth-service
server.port=5001
spring.datasource.url=jdbc:postgresql://localhost:5432/vmlts
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD:postgres123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
jwt.secret=${JWT_SECRET:vmlts_jwt_super_secret_key_2024_X}
jwt.expiration=604800000
logging.level.com.vmlts=INFO

# Gmail SMTP for password-reset emails. Replace the placeholders below with a real
# Gmail address and a 16-character App Password (Google Account > Security > App Passwords,
# requires 2-Step Verification). Never use your normal Gmail password here.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:sleepyraven777@gmail.com}
spring.mail.password=${MAIL_APP_PASSWORD:ctvgekyjgtfnrnma}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000